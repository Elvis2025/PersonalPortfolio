{
  "name": "personal-portfolio-monorepo",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix back-end\" \"npm run dev --prefix front-end\"",
    "dev:front-end": "npm run dev --prefix front-end",
    "dev:back-end": "npm run dev --prefix back-end",
    "build": "npm run install:apps && npm run build --prefix back-end && npm run build --prefix front-end",
    "install:apps": "npm ci --prefix back-end && npm ci --prefix front-end",
    "start": "npm run start --prefix back-end"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
