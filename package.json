{
  "name": "personal-portfolio-monorepo",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "dev:client": "npm run dev --prefix client",
    "dev:server": "npm run dev --prefix server",
    "build": "npm run install:apps && npm run build --prefix server && npm run build --prefix client",
    "install:apps": "npm ci --prefix server && npm ci --prefix client",
    "start": "npm run start --prefix server"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/node": "^24.3.0",
    "@types/nodemailer": "^6.4.17",
    "concurrently": "^9.0.1"
  },
  "dependencies": {
    "resend": "^6.9.2"
  }
}
